# Roadmap

## 现状（v3）

一个仓库、多个货架的 Skill 管理模型已经完整可用：

- 核心生命周期：`import` / `import-github`（从 GitHub 仓库直接收编，自动识别多 Skill 仓库并列出候选子路径）/ `track-source`（给已有 Skill 补登记来源）/ `check-updates`（批量检查登记过来源的 Skill 有没有上游更新）/ `activate` / `deactivate` / `delete`（v3 新增，停用 + 挪进系统废纸篓 + 清理 catalog/aliases 记录，见下文）/ `export`（打包成干净拷贝，供手动上传或分享）
- 场景包切换：`profile use`（`core` 永远保留，手动激活的 Skill 不受切换影响）
- 工具接入：十个已知适配器（Codex/Cursor/Gemini CLI/Claude Code/Kiro/Trae/TraeCode CN/CodeBuddy/Qoder/Windsurf）的检测与软链连接，`config/adapters/tools.tsv` 声明式扩展，面板里也能直接注册清单外的新平台
- 健康检查：`doctor`（断链、工具检测漏检、`verification` 拦截、`manual.list` 一致性、Skill 内容完整性、catalog 一致性）
- 内容质量检查：`lint`（frontmatter 完整性、description 长度、激活集合内触发词重叠）
- 退出机制：`eject`（把受管软链物化成独立真实拷贝，改前自动备份）
- 跨机器备份：`backup init` / `backup status` / `backup sync`（v3 新增，把仓库推送到私有 GitHub 仓库，换机器用 `backup init --remote <地址>` 克隆恢复，见 README）
- 面板 `dashboard`，两种模式二选一：**静态快照**（`dashboard build`，纯前端生成命令 + 复制到剪贴板，不执行任何写操作，v1 就有）；**本地服务**（`dashboard serve`，v2 新增，只监听 `127.0.0.1` + 每次启动随机端口和 session token 的本地后端，按钮点了直接生效——批量勾选常驻/移入仓库/删除三种模式、GitHub 导入向导（预览再确认，多 Skill 仓库支持勾选多个批量导入）、本地拖拽导入文件夹或 zip、检查更新、注册自定义平台、日间/夜间/跟随系统主题切换、v3 新增同步到云端按钮）。v3 新增面板界面框架的中英文切换（右上角，见下文"不做的事"里的例外说明）。
- 低上下文路由：`route` / `route-status`（实验性，需要达到命中率门槛才会自动启用，未达标时始终交给用户用中文名称确认）

### v2 → v3 的行为变化

- **新增 CLI 级别的 `delete` 命令**，回答了 v2 留下的那个开放问题（见下方旧版"可能会做"条目）。但触发动机不是"想补上命令行入口"这个体验诉求，而是修一个真实 bug：面板删除流程之前只清理了 `catalog.tsv`，没清理 `config/aliases.tsv` 里对应的行，删得越多这个文件积累的死记录越多，换机器也会带着走。`skillctl delete <id> --apply` 把"停用 + 挪进系统废纸篓 + 重建 catalog + 清理 aliases 行"收成一个命令，本地服务模式的删除按钮和静态模式复制的命令现在都调用这一个实现，不再各写一遍各漏一遍。删除仍然是挪进系统废纸篓，不是 `rm -rf`。
- **新增跨机器备份**（`backup init` / `status` / `sync`）。设计上是"给你的仓库配一个私有 GitHub 仓库 + 手动触发的 push/pull"，不是常驻后台自动同步——`backup init` 不带 `--remote` 是全新建私仓，带 `--remote <地址>` 是从已有备份克隆恢复；`backup sync` 提交本地改动、拉取远端，遇到同一个 Skill 目录本地和远端都改过时，按整个目录列出冲突、给出 `checkout --ours/--theirs` 的处理命令，不做逐行合并（Skill 内容大多是成段的 Markdown，逐行合并容易把两份不相关的改动硬拼出语义错误的结果）。
- **面板新增界面框架的中英文切换**，跟下面"不做英文文档"这条冻结原则字面上有张力——已经在维护者层面讨论过，结论是保留这条原则本身（README/ROADMAP/CLAUDE.md 等文档不会维护双语），但面板作为一个独立的 UI 产物破例支持切换，范围只覆盖框架文字（按钮、标签、表头、提示），不覆盖每个 Skill 自己的中文名称/简介、三个向导弹窗内部文字、toast/alert 这类运行时提示——那些保持中文。这是一次性讨论过的例外，不代表原则被推翻，以后新功能不要默认援引这次的先例。

### v1 → v2 的行为变化

- **`archive`/`restore` 已整体移除**，不再是核心生命周期的一部分。v1 里"下架可恢复"的定位，现在改由 `dashboard serve` 的"删除模式"承担——删除时把 Skill 目录挪进**系统废纸篓**（macOS 原生"最近删除"），不是本项目自己维护一套归档区和恢复命令。好处是不用维护一套并行的归档存储和一致性校验；代价是当时**没有 CLI 级别的删除/恢复命令**，纯命令行使用者只能 `deactivate` 停用（v3 已补上 `delete`，见上文）。
- 面板从"只读、纯前端"变成"两种模式二选一"，新增的本地服务模式第一次让面板具备写权限——`不加 GUI` 这条冻结原则本身没有变（仍然是浏览器 + 本机小进程，不是原生客户端），但如果你在 v1 时期认为"面板永远不会执行任何操作"，这个假设在 v2 不再成立，升级前请知悉。

## 不做的事（冻结项，不是"以后再做"）

以下明确不在计划内，除非有人带着具体场景说服维护者改变主意：

- **不加原生 GUI**。命令行 + 浏览器面板（本地服务模式下带写权限）就是全部界面，不打包成独立桌面客户端。
- **不做 Skill 在线安装/市场**。这是仓库管理工具，不是 Skill 分发平台。
- **不做 token/用量统计**。跟"管理 Skill 在哪、露出给谁看"这个核心问题无关。
- **不增强自动路由算法**。`route` 保持简单的关键词/别名匹配，不引入向量检索或模型调用；达不到命中率门槛就诚实地不自动选，不为了"更聪明"引入更多不确定性。
- **不做 Linux/Windows 适配**。`ditto`、`/Applications` 检测等实现都是 macOS 专属，真要支持其他平台需要重新设计文件操作层，不是加几个 if 分支的事。
- **不做英文文档**。维护者的主要使用场景是中文命令行环境；欢迎社区贡献翻译，但不会由维护者主动维护双语文档。**例外（v3）**：面板界面框架文字支持中英切换，属于 UI 功能而不是文档——README/ROADMAP/CLAUDE.md 等文档本身仍然只有中文，这条原则没有变。

## 可能会做，但没有承诺时间表

- `route`/`project` 目前是隐藏的实验性子命令（`skillctl experimental route|project`），代码和行为都稳定，只是还没决定要不要提升到一等公民命令。
- 更多官方适配器（其他常见 AI 编程工具），前提是有人验证过真实的目录结构和检测方式——参考 README「接入清单外的工具」自己先接入，验证好了欢迎提 PR。
